<script setup lang="ts">
import AppLogoIcon from '@/components/AppLogoIcon.vue';
import type { UIConfigBrand } from '@/types';
import { usePage } from '@inertiajs/vue3';
import { computed } from 'vue';

const brand = computed(() => usePage().props.brand as UIConfigBrand);
</script>

<template>
    <div
        v-if="brand.show_icon"
        class="flex aspect-square size-8 items-center justify-center rounded-md bg-sidebar-primary text-sidebar-primary-foreground"
    >
        <AppLogoIcon
            class="size-5 fill-current text-sidebar-primary-foreground"
        />
    </div>
    <div v-if="brand.show_name" class="ml-1 grid flex-1 text-left text-sm">
        <span class="mb-0.5 truncate leading-tight font-semibold"
            >Quarc Starter Kit</span
        >
    </div>
</template>
